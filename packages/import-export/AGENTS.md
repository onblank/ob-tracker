# AGENTS.md — import-export

Own `.obtracker` and generic CSV serialization/validation. Never implement Central networking. Validate checksums, row counts, IDs, enums and referential integrity before persistence.
