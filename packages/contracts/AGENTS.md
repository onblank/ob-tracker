# AGENTS.md — contracts

Also read `../../AGENTS.md` and `../../../AGENTS.md`; both remain applicable to this subtree.

Ports/interfaces only. Repository contracts expose domain/application concepts, never SQLite rows, SQL strings, Electron types or filesystem handles. Specialized read models are allowed for dashboard/search.
