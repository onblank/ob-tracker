# AGENTS.md — settings

Also read `../../AGENTS.md` and `../../../AGENTS.md`; both remain applicable to this subtree.

Typed local settings/defaults only. Settings are local-only and not part of `.obtracker`. No secrets, API keys or server config.
