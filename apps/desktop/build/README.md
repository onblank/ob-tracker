# Native build assets

Current `icon.png`, `icon.ico`, `icon.icns` and `tray-icon.png` are placeholders using the onBlank palette.

Before an official branded release, replace them with approved OB-Tracker artwork while keeping the same filenames and required platform formats. `electron-builder.yml` already points to these paths.
