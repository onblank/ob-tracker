# Brand asset drop-in folder

These files are placeholders:

- `onblank-logo.svg`
- `powered-by-onblank.svg`
- `ob-tracker-logo.svg`

When final artwork exists, replace the file contents while preserving the same filename (or update the import if changing format). The UI constrains logo display boxes in `styles.css` with explicit dimensions and `object-fit: contain`, so intrinsic artwork dimensions do not control layout.

For a zero-code swap, export the final assets as SVG with these exact filenames.
